server:
  port: 9001

eureka:
  instance:
    hostname: eureka
  client:
    # 配置此应用为注册中心，false：不向注册中心注册自己。否则启动报错
    registerWithEureka: false
    # 注册中心职责是维护服务实例，false：不检索服务。
    fetchRegistry: false
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/